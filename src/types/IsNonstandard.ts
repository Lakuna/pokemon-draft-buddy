/**
 * A legality status.
 * @public
 */
enum IsNonstandard {
	/** Custom. */
	CAP = "CAP",

	/** National Pokédex only. */
	NatDex = "NatDex",

	/** Standard. */
	Standard = "Standard"
}

export default IsNonstandard;
